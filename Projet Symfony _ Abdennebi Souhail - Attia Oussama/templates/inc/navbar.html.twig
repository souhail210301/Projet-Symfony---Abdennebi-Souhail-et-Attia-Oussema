<nav class="navbar navbar-expand-sm navbar-dark bg-dark mb-3">
    <div class="container">
        <a href="/" class="navbar-brand"></a>
        <button class="navbar-toggler" type="button" datatoggle="collapse" data-target="#mobile-nav">
            <span class="navbar-toggle-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="mobile-nav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a href="{{ path('book_list') }}" class="nav-link">Home</a>
                </li>
                {% if  is_granted('ROLE_EDITOR') %}
                    <li class="nav-item">
                    <a href="{{ path('book_create') }}" class="nav-link">Ajouter un Livre</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('author_create') }}" class="nav-link">Ajouter un auteur</a>
                </li>
                {% endif %}
                {% if not app.user %}
                    <li class="nav-item">
                        <a href="{{ path('security_login') }}" class="nav-link">Connexion</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{ path('security_logout') }}" class="nav-link">Déconnexion</a>
                    </li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a href="{{ path('admin_utilisateurs')}}" class="nav-link">Administration</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>